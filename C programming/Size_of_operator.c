// size of oparator 
#include<stdio.h>
int main(void)
{
    printf("On my system\n");
    printf("Int is:", sizeof(int));
    printf("Float is:", sizeof(float));
    printf("Char is:", sizeof(char));
    printf("Long is:", sizeof(long));
    printf("Double is:", sizeof(double));
    //done......
    return 0;
}
